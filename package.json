{
  "name": "apps_by_alicealexandra",
  "private": true,
  "scripts": {
    "tempo-immaterial": "turbo dev --filter='tempo-immaterial'",
    "anthropotpourri": "turbo dev --filter='anthropotpourri'",
    "lint": "pnpm eslint . \"!apps/** !packages/** !tooling/**\" --cache --cache-location 'node_modules/.cache/.eslintcache' --max-warnings 0",
    "lint:fix": "pnpm eslint . \"!apps/** !packages/** !tooling/**\" --fix --cache --cache-location 'node_modules/.cache/.eslintcache' --max-warnings 0"
  },
  "devDependencies": {
    "@apps-by-alice/lint": "workspace:*",
    "eslint": "^8.57.0",
    "turbo": "2.0.14"
  },
  "packageManager": "pnpm@8.7.4"
}
